# Project Build Instructions

## Build Instructions

To build the project, run the following command in the root directory:

```sh
make all
````

This command will execute the all target, which will build both part1 and part2 directories using their respective Makefiles.

## Clean Instructions
To clean the project, run the following command in the root directory:
```
make clean
```

